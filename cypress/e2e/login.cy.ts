describe('Test a login page', () => {
  it('Where click on login sould redirect to /dashboard', () => {
    cy.visit('/login')

    // cy.contains('Entrar').click()
    // cy.contains('Dashboard')
  })
})
